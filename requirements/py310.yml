name: mint-dev

channels:
  - conda-forge

dependencies:
  - python=3.10

# Setup dependencies.
  - cmake
  - cython
  - setuptools
  - tbb-devel
  - numpy
  - pip

# Core dependencies.
  - scitools-iris
  - vtk=9.2.2

# Test dependencies.
  - pytest
  - netcdf4
