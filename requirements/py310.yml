name: mint-dev

channels:
  - conda-forge

dependencies:
  - python=3.10

# Setup dependencies.
  - cmake
  - cython
  - setuptools
  - tbb-devel
  - numpy
  - pip

# Core dependencies.
  - libnetcdf
  - iris
  - vtk

# Test dependencies.
  - pytest
  - netcdf4
