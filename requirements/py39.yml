name: mint-dev

channels:
  - conda-forge

dependencies:
  - python=3.9

# Setup dependencies.
  - cmake
  - cython
  - setuptools
  - tbb-devel
  - numpy
  - pip

# Core dependencies.
  - libnetcdf
  - scitools-iris
  - vtk

# Test dependencies.
  - pytest
  - netcdf4
