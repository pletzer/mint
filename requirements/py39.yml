name: mint-dev

channels:
  - conda-forge

dependencies:
  - python=3.9

# Setup dependencies.
  - cmake
  - cython
  - setuptools
  - tbb-devel
  - numpy
  - pip

# Core dependencies.
  - libnetcdf=4.8.1
  - vtk=9.1.0

# Test dependencies.
  - pytest
  - netcdf4
