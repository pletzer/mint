name: mint-dev

channels:
  - conda-forge

dependencies:
  - python=3.8

# Setup dependencies.
  - cmake
  - cython
  - setuptools
  - tbb-devel
  - pip

# Core dependencies.
  - libnetcdf
  - numpy
  - iris
  - vtk=

# Test dependencies.
  - pytest
  - netcdf4
