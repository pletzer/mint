name: mint-dev

channels:
  - conda-forge

dependencies:
  - python=3.8

# Setup dependencies.
  - cmake
  - cython
  - setuptools
  - tbb-devel
  - pip

# Core dependencies.
  - libnetcdf
  - numpy
  - vtk=9.1.0

# Test dependencies.
  - pytest
  - netcdf4=1.5.8
