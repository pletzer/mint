name: mint-dev

channels:
  - conda-forge

dependencies:
  - python=3.7

# Setup dependencies.
  - cmake
  - cython
  - setuptools
  - tbb-devel
  - pip

# Core dependencies.
  - libnetcdf
  - numpy
  - vtk=9.0.3

# Test dependencies.
  - pytest
  - netcdf4
