name: mint-dev

channels:
  - conda-forge

dependencies:
  - python=3.7  # vtk for py39 is not available from PyPI @March 2021

# Setup dependencies.
  - cmake
  - cython
  - doxygen
  - setuptools
  - tbb-devel
  - pip

# Core dependencies.
  - libnetcdf
  - netcdf-fortran
  - numpy
  - vtk=9.0.3

# Test dependencies.
  - pytest
